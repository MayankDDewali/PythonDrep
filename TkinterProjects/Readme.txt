# Importing psutil for retrieving information on running processes and system utilization
# pip install psutil

# A Python tool for controlling the brightness of your monitor
# pip install screen-brightness-control

# allows calling functions in DLLs or shared libraries
# pip install ctypes-callable

# Pycaw library for controling audio
# pip install pycaw

# allows to define, call, and implement custom and dispatch-based COM interfaces in pure Python
# pip install comtypes

# locate the coordinates of addresses, cities, countries, and landmarks across the globe using third-party geocoders and other data sources.
# pip install geopy

# looking up the corresponding timezone for given coordinates on earth entirely offline
# pip install timezonefinder

# allows for date-time conversion and timezone calculations (online)
# pip install pytz

#  provides the Calendar and DateEntry widgets for Tkinter (offline)
# pip install tkcalendar

# provides the ability to simulate mouse cursor moves and clicks as well as keyboard button presses
# pip install pyAutoGUI